$(".follow-user-button").empty();
$(".follow-user-button").append('<%=@button_content%>')
$("#follow<%=params[:followed]%> .btn").empty();
$("#follow<%=params[:followed]%> .btn").append('<%=@button_content.split[0]%>');

if('<%=params[:popup]%>'==="true"&&'<%=@direction%>'==="add")
	{$("#count<%=params[:followed]%>").text(parseInt($("#count<%=params[:followed]%>").text())+1);
	if('<%=params[:same]%>'==="true")
		{$("#followed_link").text(parseInt($("#followed_link").text())+1);}
	}
else if('<%=params[:popup]%>'==="true")
	{$("#count<%=params[:followed]%>").text(parseInt($("#count<%=params[:followed]%>").text())-1);
	if('<%=params[:same]%>'==="true")
		{$("#followed_link").text(parseInt($("#followed_link").text())-1);}
	}
else if('<%=@direction%>'==="add")
{
$("#followers_link").text(parseInt($("#followers_link").text())+1);
$("#count<%=params[:followed]%>").text(parseInt($("#count<%=params[:followed]%>").text())+1);
}
else
{$("#followers_link").text(parseInt($("#followers_link").text())-1);
$("#count<%=params[:followed]%>").text(parseInt($("#count<%=params[:followed]%>").text())-1);
}
